import React from 'react';

/*
TODO:
if not signed in, copy should include mention of sign in, sign up and rules
if signed in, include mention of rules and picks
*/
export const LandingPage = () => (
  <div>
    <h1>Landing</h1>
  </div>
);
